/**
 * @fileoverview Firestore Security Rules for MotorLog application.
 *
 * Core Philosophy:
 * This ruleset allows any authenticated user to read and write data.
 * This is to support multi-user collaboration on the same set of vehicles.
 * Access is restricted to authenticated users only.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    // Allow read/write access to all collections for any authenticated user.
    match /{document=**} {
       allow read, write: if isSignedIn();
    }

    // User profiles remain protected. This more specific rule takes precedence.
    // Only the owner of a profile can read or write to it.
    match /users/{userId} {
      allow read, write: if isSignedIn() && request.auth.uid == userId;
    }
  }
}
